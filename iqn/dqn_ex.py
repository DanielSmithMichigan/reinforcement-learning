import numpy as np
import math

def branin(valueMin,
           valueMax,
           numAtoms,
           maxMemoryLength,
           numIntermediateLayers,
           intermediateLayerSize,
           lastLayerSize,
           learningRate,
           priorityExponent,
           epsilonInitial,
           epsilonDecay):
    print(valueMin)
    print(valueMax)
    print(numAtoms)
    print(maxMemoryLength)
    print(numIntermediateLayers)
    print(intermediateLayerSize)
    print(lastLayerSize)
    print(learningRate)
    print(priorityExponent)
    print(epsilonInitial)
    print(epsilonDecay)
    return result + noise

# Write a function like this called 'main'
def main(job_id, params):
    print params
    return -1

